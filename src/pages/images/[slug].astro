---
export async function getStaticPaths() {
  // Assuming you have a list of image slugs
  const imageSlugs = ['pine1b.jpeg', 'pine1b.jpeg', 'pine1b.jpeg'];

  return imageSlugs.map((slug) => {
    return {
      params: { slug },
    };
  });
}